{{!-- 
    Invisible dummy frame to redirect response answer 
    to this to don't reload the page after request.
--}}
<iframe style="display: none" name="dummyframe" id="dummyframe"></iframe>

<form class="form" action="/sendMessage" method="POST" target="dummyframe" >
    {{!-- Pass token back to authenticate web request --}}
    <input type="hidden" name="token" value="{{ token }}" />
    {{!-- Channel selector --}}
    <div class="select">
    <select class="slct" name="channelid">
        <option selected disabled>Sélectionner un channel</option>
        {{#each chans}}
            <option value="{{this.id}}">{{this.name}}</option>
        {{/each}}
    </select>
    </div>
    {{!-- Message text input --}}
    <textarea class="text" name="text" placeholder="Texte"></textarea>
    {{!-- Send button --}}
    <input type="submit" class="button" value="Envoyer">
</form>